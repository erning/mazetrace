[package]
name = "mazetrace"
version = "1.0.0"
edition = "2021"
description = "Animated terminal maze generation and exploration."
license = "MIT"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
crossterm = "0.29"
rand = "0.10"
ratatui = "0.30"
